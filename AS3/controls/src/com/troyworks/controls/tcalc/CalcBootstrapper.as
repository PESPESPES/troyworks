package com.troyworks.controls.tcalc { 

	/**
	 * @author Troy Gardner
	 */
	import flash.display.MovieClip;
	public class CalcBootstrapper {
		/**************************************************
		 * Called by the MTASC compiler with the '-main' flag
		 * set.
		 * 
		 */
		public static function main(container : MovieClip) : void {
			//=========== Register Classes ================/
			//Object.registerClass("CalcUI", CalcUI);
			//Object.registerClass("MCButton", MCButton);
		//	Object.registerClass("MCBackground", MCBackground);
			//========== notify parent of load =================//
		//	var loadTarget_mc:MovieClip = (container == _root || container == _level1)?container:container.parent;
		//	loadTarget_mc.nextFrame();
	    }
		
	}
}